/** Shared field styling — every text/number/date/select input in the app uses this. */
export const inputClass =
  'w-full rounded-md border border-rule bg-paper px-3 py-2 text-sm text-ink outline-none focus:border-accent'

export const labelClass = 'mb-1 block text-sm font-medium text-ink-muted'
